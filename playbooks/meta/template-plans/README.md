# Template plans (historical)

These plans document the **template's own construction** — completed work on the template
itself, retained here for provenance. They are not a downstream project's durable plans; those
live in `docs/_plans/` (see `_base/AGENTS.md`).
