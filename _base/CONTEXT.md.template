# Context Pointer

> **This is a downstream-owned root pointer.** The primary domain glossary now lives at
> `docs/resources/CONTEXT.md`, seeded from `_base/docs/resources/CONTEXT.md`.

The primary domain glossary lives at [docs/resources/CONTEXT.md](docs/resources/CONTEXT.md).

Agents should update `docs/resources/CONTEXT.md`. Keep this root `CONTEXT.md` small so legacy tooling
and quick human discovery still find the glossary without making the repo root the source of truth.

If you find an older substantive root glossary, treat it as a legacy fallback and move future glossary
edits to `docs/resources/CONTEXT.md` when practical.
